import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  // template: `<app-detail-list/>`
  // template: `<app-make-order/>`
  template: `<app-production/>`
})
export class AppComponent {
  title = 'demo-repos';
}
